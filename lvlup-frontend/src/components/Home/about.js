import React from 'react';
import { Grid } from 'semantic-ui-react';
import './homeview.css';

const About = props => (
  <div>
    <Grid>
      <Grid.Row />
    </Grid>
  </div>
);

export default About;
